#include <stdio.h>
#include <time.h>
#include <windows.h>
#include <stdlib.h>

int main( )
{
    int stringFirstMatrix, stringSecondMatrix = 0;
    int columnFirstMatrix, columnSecondMatrix = 0;
    int i, j, k, w, a, b, x, y;
    int *firstMatrix;

    printf("Size matrix:\n");
    printf("String:");
    scanf_s("%d", &stringFirstMatrix);
    printf("Column:");
    scanf_s("%d", &columnFirstMatrix);



    return 0;
}


// SetConsoleTextAttribute(GetStdHandle(STD_OUTPUT_HANDLE), FOREGROUND_BLUE);
// srand( time(0) );
//    int r = rand() % 2;
//    printf("%d", r);